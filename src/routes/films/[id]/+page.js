export function load({ params }){
    return {
        id: params.id
        // post:{
        //     title: `Title for ${params.slug} goes here`,
        //     content: `Content for ${params.slug} gore here`
        // }
    };
}